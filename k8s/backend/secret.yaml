# path: k8s/backend/secret.yaml
# version: 1.2

apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  # ========================================================================
  # Authentication
  # ========================================================================
  JWT_SECRET: "your-secret-key-change-in-production"
  
  # ========================================================================
  # Database - ArangoDB
  # ========================================================================
  ARANGO_PASSWORD: "changeDbMe123"
  ARANGO_ROOT_PASSWORD: "changeRootMe123"
  
  # ========================================================================
  # Storage - MinIO
  # ========================================================================
  MINIO_SECRET_KEY: "minioadminpwd"
  
  # ========================================================================
  # Initial Root User
  # ========================================================================
  ROOT_USER_PASSWORD: "changeme123"