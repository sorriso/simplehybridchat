# path: Makefile.test-fingerprint
# version: 1.2
# Dedicated Makefile for ssl_client_fingerprint testing
# Changes in v1.2:
# - Added deploy-all and undeploy-all commands for complete test infrastructure
# - Updated architecture: Ingress -> Caddy -> app-service/admin-service (NGINX)
# Changes in v1.1:
# - Added install-ingress and uninstall-ingress commands

.PHONY: help install-ingress uninstall-ingress deploy-all undeploy-all generate-certs deploy-ca deploy-server-tls whitelist apply-caddy test clean

.DEFAULT_GOAL := help

help: ## Show help
	@echo "SSL Client Fingerprint Testing"
	@echo "==============================="
	@echo ""
	@echo "Infrastructure Commands:"
	@echo "  make -f Makefile.test-fingerprint install-ingress   - Install NGINX Ingress Controller with patches"
	@echo "  make -f Makefile.test-fingerprint uninstall-ingress - Uninstall NGINX Ingress Controller"
	@echo ""
	@echo "Deployment Commands:"
	@echo "  make -f Makefile.test-fingerprint deploy-all        - Deploy complete test infrastructure"
	@echo "  make -f Makefile.test-fingerprint undeploy-all      - Remove complete test infrastructure"
	@echo ""
	@echo "Testing Commands:"
	@echo "  make -f Makefile.test-fingerprint generate-certs    - Generate test CA and client certificates"
	@echo "  make -f Makefile.test-fingerprint deploy-ca         - Generate ingress/ingress-client-ca-secret.yaml"
	@echo "  make -f Makefile.test-fingerprint deploy-server-tls - Generate ingress/ingress-tls-secret.yaml"
	@echo "  make -f Makefile.test-fingerprint whitelist         - Whitelist client.crt fingerprint in Caddy"
	@echo "  make -f Makefile.test-fingerprint apply-caddy       - Apply Caddy config and restart pod"
	@echo "  make -f Makefile.test-fingerprint test              - Run fingerprint capture test"
	@echo "  make -f Makefile.test-fingerprint clean             - Remove test certificates and CA secret"
	@echo ""
	@echo "Complete Workflow:"
	@echo "  1. make -f Makefile.test-fingerprint install-ingress"
	@echo "  2. make -f Makefile.test-fingerprint generate-certs"
	@echo "  3. make -f Makefile.test-fingerprint deploy-ca           (generates ingress/ingress-client-ca-secret.yaml)"
	@echo "  4. make -f Makefile.test-fingerprint deploy-server-tls   (generates ingress/ingress-tls-secret.yaml)"
	@echo "  5. make -f Makefile.test-fingerprint deploy-all          (deploys all resources including secrets)"
	@echo "  6. make -f Makefile.test-fingerprint whitelist           (optional, whitelists client.crt)"
	@echo "  7. make -f Makefile.test-fingerprint apply-caddy         (if whitelist was done)"
	@echo "  8. make -f Makefile.test-fingerprint test"
	@echo ""
	@echo "Note: CA is created once and reused. To add new clients:"
	@echo "  - Generate: ./scripts/generate-additional-client.sh <client-name>"
	@echo "  - Whitelist: ./scripts/whitelist-fingerprint.sh test-certs/<client-name>.crt"
	@echo "  - Apply: make -f Makefile.test-fingerprint apply-caddy"
	@echo ""
	@echo "Architecture:"
	@echo "  Browser -> Ingress (NodePort) -> Caddy -> app-service or admin-service (NGINX)"
	@echo ""

# =============================================================================
# Infrastructure Commands
# =============================================================================

install-ingress: ## Install NGINX Ingress Controller v1.14.1 with patches
	@echo "🔄 Installing NGINX Ingress Controller v1.14.1..."
	@kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.14.1/deploy/static/provider/cloud/deploy.yaml
	@echo "✅ NGINX Ingress Controller manifest applied"
	@echo ""
	@echo "⏳ Waiting for ingress controller to be ready (this may take a few minutes)..."
	@kubectl wait --namespace ingress-nginx \
		--for=condition=ready pod \
		--selector=app.kubernetes.io/component=controller \
		--timeout=300s
	@echo ""
	@echo "✅ NGINX Ingress Controller is ready!"
	@echo ""
	@echo "🔧 Applying patches to enable snippet annotations..."
	@echo "   → Patching deployment..."
	@kubectl patch deployment ingress-nginx-controller -n ingress-nginx \
		--type='json' --patch-file k8s/nginx-ingress-controller-patch.json
	@echo "   → Patching configmap..."
	@kubectl patch configmap ingress-nginx-controller -n ingress-nginx \
		--type='merge' --patch-file k8s/nginx-ingress-configmap-patch.json
	@echo "✅ Patches applied successfully"
	@echo ""
	@echo "♻️  Restarting ingress controller..."
	@kubectl delete pods -n ingress-nginx -l app.kubernetes.io/component=controller
	@echo "✅ Pod deleted, waiting for new pod to be ready..."
	@echo ""
	@echo "⏳ Waiting for controller to restart with new configuration..."
	@kubectl rollout status deployment ingress-nginx-controller -n ingress-nginx --timeout=120s
	@echo ""
	@echo "✅ NGINX Ingress Controller patched and ready!"
	@echo ""
	@echo "📊 Ingress controller status:"
	@kubectl get pods -n ingress-nginx
	@echo ""
	@kubectl get svc -n ingress-nginx

uninstall-ingress: ## Uninstall NGINX Ingress Controller
	@echo "🛑 Uninstalling NGINX Ingress Controller..."
	@kubectl delete -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.14.1/deploy/static/provider/cloud/deploy.yaml 2>/dev/null || echo "Already uninstalled"
	@echo "✅ NGINX Ingress Controller uninstalled"
	@echo ""
	@echo "📊 Checking remaining resources..."
	@kubectl get namespace ingress-nginx 2>/dev/null || echo "Namespace ingress-nginx removed"

# =============================================================================
# Deployment Commands
# =============================================================================

deploy-all: ## Deploy complete test infrastructure (namespace + app + admin + caddy + ingress)
	@echo "🚀 Deploying complete test infrastructure..."
	@echo ""
	@echo "📦 Creating namespace chatbot..."
	@kubectl apply -f namespace.yaml
	@echo ""
	@echo "📦 Deploying resources via kustomize..."
	@kubectl apply -k .
	@echo ""
	@echo "⏳ Waiting for pods to be ready..."
	@kubectl wait --for=condition=ready pod -l app=app -n chatbot --timeout=60s 2>/dev/null || true
	@kubectl wait --for=condition=ready pod -l app=admin -n chatbot --timeout=60s 2>/dev/null || true
	@kubectl wait --for=condition=ready pod -l app=caddy -n chatbot --timeout=60s 2>/dev/null || true
	@echo ""
	@echo "✅ Test infrastructure deployed!"
	@echo ""
	@echo "📊 Current status:"
	@kubectl get pods -n chatbot
	@echo ""
	@kubectl get svc -n chatbot
	@echo ""
	@kubectl get ingress -n chatbot
	@echo ""
	@echo "💡 Next steps:"
	@echo "   1. Add to /etc/hosts: echo '127.0.0.1 app.kube.local admin.kube.local' | sudo tee -a /etc/hosts"
	@echo "   2. Generate certificates: make -f Makefile.test-fingerprint generate-certs"
	@echo "   3. Deploy CA: make -f Makefile.test-fingerprint deploy-ca"
	@echo "   4. Run test: make -f Makefile.test-fingerprint test"

undeploy-all: ## Remove complete test infrastructure
	@echo "🛑 Removing test infrastructure..."
	@kubectl delete -k . 2>/dev/null || echo "Resources already removed"
	@kubectl delete namespace chatbot 2>/dev/null || echo "Namespace already removed"
	@echo "✅ Test infrastructure removed"

# =============================================================================
# Testing Commands
# =============================================================================

generate-certs: ## Generate test CA and client certificates
	@echo "🔐 Generating test certificates..."
	@chmod +x scripts/generate-test-certs.sh
	@./scripts/generate-test-certs.sh

deploy-ca: ## Generate ingress/ingress-client-ca-secret.yaml from CA certificate
	@if [ ! -f test-certs/ca.crt ]; then \
		echo "❌ Error: CA certificate not found!"; \
		echo "   Run 'make -f Makefile.test-fingerprint generate-certs' first"; \
		exit 1; \
	fi
	@echo "📝 Generating ingress/ingress-client-ca-secret.yaml..."
	@mkdir -p ingress
	@echo "# path: ingress/ingress-client-ca-secret.yaml" > ingress/ingress-client-ca-secret.yaml
	@echo "# Generated by: make -f Makefile.test-fingerprint deploy-ca" >> ingress/ingress-client-ca-secret.yaml
	@echo "# This file is auto-generated from test-certs/ca.crt" >> ingress/ingress-client-ca-secret.yaml
	@echo "# Do not edit manually - regenerate with the command above" >> ingress/ingress-client-ca-secret.yaml
	@echo "" >> ingress/ingress-client-ca-secret.yaml
	@echo "apiVersion: v1" >> ingress/ingress-client-ca-secret.yaml
	@echo "kind: Secret" >> ingress/ingress-client-ca-secret.yaml
	@echo "metadata:" >> ingress/ingress-client-ca-secret.yaml
	@echo "  name: client-ca-secret" >> ingress/ingress-client-ca-secret.yaml
	@echo "type: Opaque" >> ingress/ingress-client-ca-secret.yaml
	@echo "stringData:" >> ingress/ingress-client-ca-secret.yaml
	@echo "  ca.crt: |" >> ingress/ingress-client-ca-secret.yaml
	@sed 's/^/    /' test-certs/ca.crt >> ingress/ingress-client-ca-secret.yaml
	@echo "✅ File generated: ingress/ingress-client-ca-secret.yaml"
	@echo ""
	@echo "💡 To deploy: kubectl apply -f ingress/ingress-client-ca-secret.yaml"

deploy-server-tls: ## Generate ingress/ingress-tls-secret.yaml from server certificate
	@if [ ! -f test-certs/server.crt ] || [ ! -f test-certs/server.key ]; then \
		echo "❌ Error: Server TLS certificate not found!"; \
		echo "   Run 'make -f Makefile.test-fingerprint generate-certs' first"; \
		exit 1; \
	fi
	@echo "📝 Generating ingress/ingress-tls-secret.yaml..."
	@mkdir -p ingress
	@echo "# path: ingress/ingress-tls-secret.yaml" > ingress/ingress-tls-secret.yaml
	@echo "# Generated by: make -f Makefile.test-fingerprint deploy-server-tls" >> ingress/ingress-tls-secret.yaml
	@echo "# This file is auto-generated from test-certs/server.crt and test-certs/server.key" >> ingress/ingress-tls-secret.yaml
	@echo "# Do not edit manually - regenerate with the command above" >> ingress/ingress-tls-secret.yaml
	@echo "" >> ingress/ingress-tls-secret.yaml
	@echo "apiVersion: v1" >> ingress/ingress-tls-secret.yaml
	@echo "kind: Secret" >> ingress/ingress-tls-secret.yaml
	@echo "metadata:" >> ingress/ingress-tls-secret.yaml
	@echo "  name: server-tls-secret" >> ingress/ingress-tls-secret.yaml
	@echo "type: kubernetes.io/tls" >> ingress/ingress-tls-secret.yaml
	@echo "stringData:" >> ingress/ingress-tls-secret.yaml
	@echo "  tls.crt: |" >> ingress/ingress-tls-secret.yaml
	@sed 's/^/    /' test-certs/server.crt >> ingress/ingress-tls-secret.yaml
	@echo "  tls.key: |" >> ingress/ingress-tls-secret.yaml
	@sed 's/^/    /' test-certs/server.key >> ingress/ingress-tls-secret.yaml
	@echo "✅ File generated: ingress/ingress-tls-secret.yaml"
	@echo ""
	@echo "💡 To deploy: kubectl apply -f ingress/ingress-tls-secret.yaml"
	@echo ""
	@echo "💡 To use this certificate in your ingress:"
	@echo "   spec:"
	@echo "     tls:"
	@echo "       - hosts:"
	@echo "           - *.kube.local"
	@echo "         secretName: server-tls-secret"

whitelist: ## Whitelist client.crt fingerprint in Caddy configmap
	@if [ ! -f test-certs/client.crt ]; then \
		echo "❌ Error: Client certificate not found!"; \
		echo "   Run 'make -f Makefile.test-fingerprint generate-certs' first"; \
		exit 1; \
	fi
	@echo "🔐 Whitelisting client certificate fingerprint..."
	@chmod +x scripts/whitelist-fingerprint.sh
	@./scripts/whitelist-fingerprint.sh test-certs/client.crt
	@echo ""
	@echo "💡 Don't forget to apply: make -f Makefile.test-fingerprint apply-caddy"

apply-caddy: ## Apply Caddy configmap and restart pod
	@echo "📦 Applying Caddy configuration..."
	@kubectl apply -f caddy/configmap.yaml
	@echo "🔄 Restarting Caddy pod..."
	@kubectl delete pod -l app=caddy -n chatbot
	@echo "⏳ Waiting for Caddy to be ready..."
	@kubectl wait --for=condition=ready pod -l app=caddy -n chatbot --timeout=60s 2>/dev/null || echo "Caddy is starting..."
	@echo "✅ Caddy configuration applied and pod restarted"

test: ## Run fingerprint capture test with curl
	@if [ ! -f test-certs/client.crt ]; then \
		echo "❌ Error: Client certificate not found!"; \
		echo "   Run 'make -f Makefile.test-fingerprint generate-certs' first"; \
		exit 1; \
	fi
	@echo "🧪 Running fingerprint test..."
	@echo ""
	@echo "⚠️  Make sure '127.0.0.1 app.kube.local admin.kube.local' is in /etc/hosts"
	@echo ""
	@chmod +x scripts/test-fingerprint.sh
	@./scripts/test-fingerprint.sh

clean: ## Remove test certificates and CA secret
	@echo "🗑️  Removing test certificates..."
	@rm -rf test-certs/
	@echo "🗑️  Removing CA secret..."
	@kubectl delete secret test-ca-secret -n chatbot 2>/dev/null || echo "No CA secret found"
	@echo "✅ Test resources cleaned"