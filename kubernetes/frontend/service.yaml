# path: k8s/frontend/service.yaml
# version: 1.0
# Frontend internal service

apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: chatbot
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
