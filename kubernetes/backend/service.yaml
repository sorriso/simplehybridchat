# path: k8s/backend/service.yaml
# version: 1.0
# Backend API internal service

apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: chatbot
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
