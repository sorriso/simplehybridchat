# File: OpenDocSeal/infrastructure/kube/STR-secret.yaml
# Version: 1
apiVersion: v1
kind: Secret
metadata:
  name: opendocseal-str-secret
type: Opaque
stringData:
  # MinIO admin credentials (required by MinIO)
  MINIO_ROOT_USER: "admin"
  MINIO_ROOT_PASSWORD: "MyAdmin01"
  # Application-specific bucket configuration
  BUCKET_NAME: "opendocseal-documents"
  ENABLE_VERSIONING: "1"
  # Application access credentials for API
  APP_ACCESS_KEY: "opendocseal_app"
  APP_SECRET_KEY: "OpDs_App_Secret_2025_VeryLongAndSecure!"