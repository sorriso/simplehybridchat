// path: tests/mocks/config.ts
// version: 1 - Shared configuration for MSW handlers

/**
 * API base URL used by the application's apiClient
 * Must match the URL in src/lib/api/client.ts
 */
export const API_BASE_URL = 'http://localhost:8000'