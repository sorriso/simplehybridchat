# path: testIngress/caddy/deployment.yaml
# version: 1.1
# Caddy reverse proxy deployment
# Changes in v1.1:
# - Updated init containers to wait for app and admin services

apiVersion: apps/v1
kind: Deployment
metadata:
  name: caddy
  namespace: chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: caddy
  template:
    metadata:
      labels:
        app: caddy
    spec:
      initContainers:
        - name: wait-for-app
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              until nc -z app-service 80; do
                echo "Waiting for app-service...";
                sleep 2;
              done
        - name: wait-for-admin
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              until nc -z admin-service 80; do
                echo "Waiting for admin-service...";
                sleep 2;
              done
      containers:
        - name: caddy
          image: caddy:2-alpine

          ports:
            - containerPort: 3000
              name: http

          volumeMounts:
            - name: caddy-config
              mountPath: /etc/caddy/Caddyfile
              subPath: Caddyfile

          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"

          # livenessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3000
          #     httpHeaders:
          #       - name: Host
          #         value: localhost
          #   initialDelaySeconds: 10
          #   periodSeconds: 10

          # readinessProbe:
          #   httpGet:
          #     path: /health
          #     port: 3000
          #     httpHeaders:
          #       - name: Host
          #         value: localhost
          #   initialDelaySeconds: 5
          #   periodSeconds: 5

      volumes:
        - name: caddy-config
          configMap:
            name: caddy-config